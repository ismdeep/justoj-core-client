#include <stdio.h>
int main()
{
    int n,m,i,s;
    scanf("%d",&n);
    if (n>=3)
    {
        s=3;m=2;
    for(i=3;i<=n;i++)
    {

      if (i>s) {s=s+i;m++;}
    }
    }
    else if (n==1) m=1;
          else m=2;
    printf("%d\n",m);
    return 0;
}